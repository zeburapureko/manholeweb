<!DOCTYPE html>
<html lang="ja">

<head>
  <meta http-equiv="content-type" 
    content="text/html; charset=UTF-8">
  <title><%= title %></title>
  <link rel="stylesheet" 
  href="https://stackpath.bootstrapcdn.com/bootstrap/4.4.1/css/bootstrap.min.css"
  crossorigin="anonymous">
  <link rel='stylesheet' 
    href='/stylesheets/style.css' />
</head>

<body class="container">

  <header>
    <h1 class="display-4">
      <%= title %></h1>
      <h2><%= userName %></h2>
  </header>
  <div role="main">
    
    <table class="table">
        <p class="mt-4"><a href="/equip/add/<%=userName %>/<%=userId %>">&lt;&lt; 端末追加&gt;&gt;</a></p>
      <% for(var i in content) { %>
      <tr>
        <% var obj = content[i]; %>
        <th><%= obj.id %></th>
        <!--<td><%= obj.name %></td>-->
        <td><%= obj.tno %></td>
      </tr>
      <% } %>
    </table>
  </div>
</body>

</html>